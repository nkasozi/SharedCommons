using Microsoft.VisualStudio.TestTools.UnitTesting;
using SharedCommons;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace SharedCommons.Tests
{
    [TestClass()]
    public class SharedCommonsTests
    {
        [TestMethod()]
        public void CheckForNullsTest()
        {
            //SystemAffected systemAffected = new SystemAffected();
            //systemAffected.ChangeRequestId = "1234";
            //systemAffected.SystemCode = "TEST";
            //systemAffected.TypeOfChange = "test";
            //string propertiesThatCanBeNull = $"{nameof(systemAffected.Id)}|{nameof(systemAffected.Details)}";
            //string result = SharedCommons.CheckForNulls(systemAffected, propertiesThatCanBeNull);

            //Assert.AreEqual(result,SharedCommonsGlobals.SUCCESS_STATUS_TEXT);
        }
    }
}